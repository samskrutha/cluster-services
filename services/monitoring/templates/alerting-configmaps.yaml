apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-alert-rules
  labels:
    app: grafana
    release: kube-prometheus-stack
  namespace: monitoring
data:
  Test-Alert.yaml: |-
    {{ .Files.Get "services/monitoring/alerting/test.yaml" | indent 4 }}