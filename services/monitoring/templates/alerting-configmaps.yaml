apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-alert-rules
  labels:
    app: grafana
    release: kube-prometheus-stack
  namespace: monitoring
data:
  test.yaml: |-
    {{ .Files.Get "monitoring/alerting/test.yaml" | indent 4 }}
    